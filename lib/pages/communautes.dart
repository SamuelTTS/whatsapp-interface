import 'package:flutter/material.dart';

class Communautes extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Center(
      child: Text("Communautes page"),
    );
  }
}